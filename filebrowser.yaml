server:
  port: 8080
  database: "/app/filebrowser.db"
  cacheDir: "/app/filebrowser-cache"
  sources:
    - path: /workspace
      name: Workspace
    - path: /
      name: root
auth:
  methods:
    noauth: true
